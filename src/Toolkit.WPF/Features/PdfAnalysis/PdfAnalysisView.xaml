<UserControl x:Class="Toolkit.WPF.Features.PdfAnalysis.PdfAnalysisView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20" MaxWidth="1000">

            <!-- Folder picker -->
            <Border Style="{StaticResource Card}" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="Source Folder" Style="{StaticResource FieldLabel}"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding FolderPath, UpdateSourceTrigger=PropertyChanged}"
                                 Margin="0,0,8,0"/>
                        <Button Grid.Column="1" Content="Browse" Style="{StaticResource SecondaryButton}"
                                Command="{Binding BrowseFolderCommand}" Margin="0,0,8,0"/>
                        <Button Grid.Column="2" Content="Analyze" Style="{StaticResource PrimaryButton}"
                                Command="{Binding AnalyzeCommand}" Margin="0,0,8,0"
                                IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBool}}"/>
                        <Button Grid.Column="3" Content="Cancel" Style="{StaticResource SecondaryButton}"
                                Command="{Binding CancelCommand}"
                                IsEnabled="{Binding IsRunning}"/>
                    </Grid>
                    <!-- Progress -->
                    <ProgressBar Value="{Binding ProgressPercent}" Maximum="100" Height="4"
                                 Margin="0,10,0,4"
                                 Visibility="{Binding IsRunning, Converter={StaticResource BoolToVisibility}}"/>
                    <TextBlock Text="{Binding StatusMessage}" FontSize="12" Foreground="#64748B"/>
                </StackPanel>
            </Border>

            <!-- Metadata editor -->
            <Border Style="{StaticResource Card}" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="Apply Metadata to Selected PDFs" Style="{StaticResource FieldLabel}"
                               FontSize="13" Margin="0,0,0,10"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,8,0">
                            <TextBlock Text="Title" Style="{StaticResource FieldLabel}"/>
                            <TextBox Text="{Binding MetadataTitle, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="0,0,8,0">
                            <TextBlock Text="Author" Style="{StaticResource FieldLabel}"/>
                            <TextBox Text="{Binding MetadataAuthor, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2" Margin="0,0,8,0">
                            <TextBlock Text="Subject" Style="{StaticResource FieldLabel}"/>
                            <TextBox Text="{Binding MetadataSubject, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                        <Button Grid.Column="3" Content="Apply" Style="{StaticResource PrimaryButton}"
                                Command="{Binding ApplyMetadataCommand}" VerticalAlignment="Bottom"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Results table -->
            <Border Style="{StaticResource Card}">
                <DataGrid ItemsSource="{Binding Documents}" AutoGenerateColumns="False"
                          CanUserAddRows="False" CanUserDeleteRows="False"
                          HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                          BorderThickness="0" Background="Transparent"
                          MaxHeight="420">
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}"
                                                Header="✓" Width="40"/>
                        <DataGridTextColumn Header="File Name"  Binding="{Binding FileName}"  Width="*" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Pages"      Binding="{Binding PageCount}" Width="60" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Page Sizes" Binding="{Binding PageSizes}" Width="120" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Title"      Binding="{Binding Title}"     Width="150" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Author"     Binding="{Binding Author}"    Width="120" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Subject"    Binding="{Binding Subject}"   Width="120" IsReadOnly="True"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
